#pragma kernel CSMain

RWStructuredBuffer<float> _Buffer;
RWTexture2D<float> _Result;

[numthreads(32, 32,1)]
void CSMain (uint3 DTid : SV_DispatchThreadID)
{
	_Result[DTid.xy] = _Buffer[DTid.y * 512 + DTid.x] / 256.0;
}
